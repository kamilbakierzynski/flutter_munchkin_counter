import 'package:flutter/material.dart';
import 'package:munchkin_level_counter/colors.dart';

class AppIcons {
  final Icon addIcon = Icon(
    Icons.add, size: 40.0, color: AppColors().textColor,);
  final Icon minusIcon = Icon(
    Icons.remove, size: 40.0, color: AppColors().textColor,);
}